#!/usr/bin/env python
# -*- coding: utf-8 -*-
# Author:Mario
import math

print(pow(2, 3))
print(abs(-10))
print(round(2 / 3))  # 圆整到最接近的整数,如果需要用到向下取整可以用floor,ceil则相反
print(math.floor(32.9))
print(math.ceil(33.2))  # ceil返回的是大于或等于给定数的最小数
# __future__把下一个新版本特性到当前版本,可以测试新版本的特性
